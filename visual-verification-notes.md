# Visual verification notes — 2026-08-16

- `/customer-portal/invalid-token` renders the intended Arabic RTL invalid/expired link state with a clear security message.
- `/sales` screenshot in the sandbox preview was blank/loading because the preview session did not have an authenticated workspace; this is not evidence of a production rendering failure. The route remains protected by the existing application authentication flow.
- TypeScript and Vitest completed successfully after the portal/approval changes: 57 test files, 123 passed and 1 skipped (SMTP integration is opt-in).

## مراجعة إضافية لبوابة العميل

- تم التقاط المسار `/customer-portal/invalid-preview-token` على سطح المكتب بعرض 1280px وعلى الجوال بعرض 390px.
- الخلفية والألوان المؤسسية مناسبة، لكن الرمز غير الصالح ظل في حالة تحميل ضمن لقطة المعاينة بسبب انتظار طلب tRPC؛ يلزم التحقق من ظهور حالة الخطأ بعد اكتمال الطلب في الإنتاج.
- لم يتم إنشاء رمز بوابة أو مستند عميل حقيقي لغرض المعاينة، حفاظاً على عدم إنشاء بيانات اختبار في الإنتاج.
- اختبار الدخول الفعلي سيُعاد بعد نشر إصلاح عزل `securityEvents`.

## لوحة العمل حسب الدور

- ظهرت لوحة «الرئيس التنفيذي / مدير الشركة» في الصفحة الرئيسية مع بطاقة الدور وثلاثة مسارات تشغيلية واضحة.
- ظهر مسار المبيعات بتصميم متجاوب، واختصار «عميل وجهة اتصال جديدة» واختصار إنشاء عرض سعر أو فاتورة، مع نموذج العميل الموحد.
- قائمة التنقل ما زالت تعرض الوحدات العامة لحساب المدير؛ التصفية الخاصة بالأدوار المقيدة تعتمد على رمز عضوية مساحة العمل من الخادم.
- تم التحقق من البناء والاختبارات بعد إضافة لوحة الدور والعزل الواجهاتي.

## مؤشرات الأداء الرئيسية

- ظهرت بطاقة «مؤشرات إنتاجيتي» ضمن الصفحة الرئيسية، وتعرض حالة عدم وجود مؤشرات مخصصة دون أرقام تجريبية.
- ظهرت بطاقة «تخصيص مؤشر جديد» للإدارة مع اسم المؤشر ومصدر القياس والهدف والفترة والدور.
- الأرقام المعروضة تعتمد على استعلامات KPI الفعلية، ولا يتم إنشاء بيانات تجريبية تلقائياً.

## تجربة المستخدم — بوابة العميل

- حالة الرابط غير الصالح على الجوال تظهر كبطاقة مركزة واضحة، مع رسالة عربية مطمئنة وعدم كشف أي بيانات.
- الواجهة الجديدة تستخدم هوية لونية هادئة، ملخصات للمستندات، حالات دفع واضحة، وتخطيطاً متجاوباً للعرض على الشاشات الصغيرة.
