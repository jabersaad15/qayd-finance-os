# Production verification

- Login at `https://qayd.tech/login` succeeded for `sales-supervisor-test` after accepting legal consent and setting a new temporary password.
- The production session showed the supervisor role and opened `/sales` successfully.
- Production `/sales` still rendered the older Sales Desk with the invoice issuance control and did not show the new Sales Supervisor Workspace cards. This indicates the managed project checkpoint is saved, but the external VPS deployment has not yet been synchronized with the latest source.
- The next required action is to sync the latest source to the known VPS deployment, rebuild/restart the container, then repeat the production check and verify `/billing` is denied for the sales supervisor.
