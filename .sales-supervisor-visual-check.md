# Visual verification — Sales Supervisor Workspace

- Desktop `/sales` at 1280x720: the supervisor header, KPI cards, intervention panel, team table, pipeline, quote review, insight cards, and reassignment controls rendered without visible clipping.
- Mobile `/sales` at 390x844: the page remained vertically scrollable, cards stacked, forms remained readable, and dense tables were contained in horizontal-scroll regions.
- The sidebar stayed limited to the sales area for the supervisor role in the captured preview.
- Follow-up validation still required: full test suite after the latest insight-card change, production build, checkpoint, and deployment verification.
