# تحقق تقرير أداء المبيعات

تم تشغيل المعاينة على مسار `/sales` بسطح مكتب 1280×720 مع التقاط الصفحة كاملة. ظهر التقرير بعد لوحة CRM مباشرة، واتسق مع الهوية البصرية الخضراء الداكنة، وظهرت المرشحات وقمع التحويل وترتيب المندوبين ضمن التدفق العام. ظهرت الصفحة بحالة بيانات فارغة آمنة دون مؤشرات وهمية؛ وهذا متوقع عند عدم وجود مساحة عمل مهيأة في جلسة المعاينة.

تشغيل `pnpm check` نجح دون أخطاء TypeScript. تشغيل `pnpm test --run` نجح بإجمالي 52 ملف اختبار و110 اختبارات. الاختبار الجديد `server/finance/salesPerformanceReports.component.test.tsx` يثبت ظهور عنوان التقرير، مؤشرات القيمة، قمع التحويل، مسؤول المبيعات ومرشح التاريخ.

ملاحظة تشغيلية: تعذر تطبيق قيود المفاتيح الأجنبية الثلاثة الخاصة بسجل تاريخ مراحل الفرص في TiDB أثناء الترحيل، بينما تم إنشاء الجدول وتطبيق فهارسه وفهارس العزل. إجراءات الخادم تطبق عزل `tenantId` و`companyId` والتحقق من الصلاحيات، لذلك لا يتعطل التقرير. يجب إعادة فحص هذه القيود عند مزامنة مخطط VPS.

تم كذلك التقاط `/sales` على شاشة 390×844. المكوّنات تتكدس عموديًا، وتبقى الفلاتر والجداول داخل حاويات قابلة للتمرير الأفقي عند الحاجة، دون ظهور تجاوز أفقي خارج الصفحة. تم الحفاظ على الأرقام والقيم المالية باتجاه LTR داخل الواجهة العربية.
